interface:
  display_name: "DeepSeek Harness Delegator"
  short_description: "Async DeepSeek delegation for parallel Codex work"
  default_prompt: "Use $delegate-to-deepseek-harness to asynchronously delegate the smallest suitable workstream, continue independent Codex work, and collect the verified result when needed."
